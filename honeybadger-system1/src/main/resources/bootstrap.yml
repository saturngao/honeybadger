spring:
  application:
    name: springcloud-nacos-discovery-provider
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      config:
        enabled: true
        register-enabled: true
        server-addr: @nacos.server.registry@
        file-extension: yml



